<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender with traceId from MDC -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-}] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Rolling file appender: 10 MB per file, keep 7 files -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/repolens.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/repolens.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-}] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Default: INFO for everything, suppress SQL logging -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Dev profile: enable SQL debug logging via Slf4jImpl -->
    <springProfile name="dev">
        <logger name="com.repolens" level="DEBUG"/>
        <logger name="org.mybatis" level="DEBUG"/>
    </springProfile>

    <!-- Reduce noise from framework internals -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.apache.rocketmq" level="WARN"/>
    <logger name="io.milvus" level="WARN"/>
    <logger name="com.baomidou.mybatisplus" level="WARN"/>

</configuration>
